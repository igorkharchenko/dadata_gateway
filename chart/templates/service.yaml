apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dadata.fullname" . }}
  labels:
    {{- include "helm-dadata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dadata.selectorLabels" . | nindent 4 }}
